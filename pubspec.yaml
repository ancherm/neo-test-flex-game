name: neo_test_flex_game
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  # sqflite: ^2.0.0+3
  # path: ^1.8.0
  floor: ^1.2.0
  floor_annotation: ^1.2.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  floor_generator: ^1.2.0
  build_runner: ^2.1.4


flutter:

  uses-material-design: true
